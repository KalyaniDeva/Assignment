package week1.day2Assignment2;

public class NegTopostive {

	public static void main(String[] args) {
		int num=-50; // 1. Initialize an integer with a negative number like, int number = -40;

		if(num<0) { //2. Check if the number is a negative number 
			//Hint : any number that is lesser than zero is a negative number
			
		}
			int x = num* -1; 							//3. If so, convert the number to a positive number.
		System.out.println(num+" is converted to "+x);	//4. Print as below "The number -40 is converted to 40"	
	}

}
